# Set to the full public URL before the production build. No trailing slash.
# Supports a subpath; Next.js basePath is derived automatically.
# Leave unset for local development and non-indexable previews.
SITE_URL=
